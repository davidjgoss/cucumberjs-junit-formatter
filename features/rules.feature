Feature: rule keyword

  Rule: rule works okay

    Example: basic example that passes
      When stuff happens
      Then it passes

    Example: basic example that fails
      When stuff happens
      Then it fails
